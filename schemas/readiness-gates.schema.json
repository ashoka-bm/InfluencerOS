{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://influencer-os.local/schemas/readiness-gates.schema.json",
  "title": "ReadinessMilestones",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "readiness_gates_id",
    "creator_profile_id",
    "creator_slug",
    "updated_on",
    "milestones",
    "permissions"
  ],
  "properties": {
    "readiness_gates_id": {
      "type": "string",
      "pattern": "^readiness_gates_[a-zA-Z0-9_-]+$"
    },
    "creator_profile_id": {
      "type": "string",
      "pattern": "^creator_[a-zA-Z0-9_-]+$"
    },
    "creator_slug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "updated_on": {
      "type": "string",
      "format": "date"
    },
    "milestones": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profile", "foundation", "strategy", "production"],
      "properties": {
        "profile": { "$ref": "#/definitions/milestone" },
        "foundation": { "$ref": "#/definitions/foundation_milestone" },
        "strategy": { "$ref": "#/definitions/milestone" },
        "production": { "$ref": "#/definitions/milestone" }
      }
    },
    "permissions": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "creator_image_generation_allowed",
        "creator_video_generation_allowed",
        "spoken_voice_generation_allowed"
      ],
      "properties": {
        "creator_image_generation_allowed": { "type": "boolean" },
        "creator_video_generation_allowed": { "type": "boolean" },
        "spoken_voice_generation_allowed": { "type": "boolean" }
      }
    }
  },
  "definitions": {
    "milestone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "approved_on", "approved_by", "blockers", "waivers"],
      "properties": {
        "status": { "$ref": "#/definitions/milestone_status" },
        "approved_on": { "$ref": "#/definitions/approval_date" },
        "approved_by": { "$ref": "#/definitions/approval_actor" },
        "blockers": { "$ref": "#/definitions/blocker_list" },
        "waivers": { "$ref": "#/definitions/waiver_list" }
      }
    },
    "foundation_milestone": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "approved_on", "approved_by", "mode", "blockers", "waivers"],
      "properties": {
        "status": { "$ref": "#/definitions/milestone_status" },
        "approved_on": { "$ref": "#/definitions/approval_date" },
        "approved_by": { "$ref": "#/definitions/approval_actor" },
        "mode": {
          "type": ["string", "null"],
          "enum": ["media_ready", "prompt_ready", null]
        },
        "blockers": { "$ref": "#/definitions/blocker_list" },
        "waivers": { "$ref": "#/definitions/waiver_list" }
      }
    },
    "milestone_status": {
      "type": "string",
      "enum": ["not_started", "in_progress", "blocked", "ready", "waived"]
    },
    "approval_date": {
      "type": ["string", "null"],
      "format": "date"
    },
    "approval_actor": {
      "type": ["string", "null"],
      "enum": ["user", null]
    },
    "blocker_list": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "waiver_list": {
      "type": "array",
      "items": { "$ref": "#/definitions/waiver" }
    },
    "waiver": {
      "type": "object",
      "additionalProperties": false,
      "required": ["waiver_id", "reason", "approved_on", "approved_by"],
      "properties": {
        "waiver_id": {
          "type": "string",
          "pattern": "^waiver_[a-zA-Z0-9_-]+$"
        },
        "reason": { "type": "string", "minLength": 1 },
        "approved_on": { "type": "string", "format": "date" },
        "approved_by": { "type": "string", "enum": ["user"] }
      }
    }
  }
}

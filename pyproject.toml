[project]
name = "influencer-os"
version = "0.1.0"
description = "Local-first file workflow contracts for InfluencerOS."
requires-python = ">=3.11"

[tool.influencer-os]
test-command = "python3 -m unittest discover -s tests"
example-validation-command = "python3 -m influencer_os validate examples"
